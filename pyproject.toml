[project]
name = "insideout"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
scaling = ["gmpy2>=2.1"]
validation = ["sympy>=1.12"]

[tool.pytest.ini_options]
testpaths = ["tests"]